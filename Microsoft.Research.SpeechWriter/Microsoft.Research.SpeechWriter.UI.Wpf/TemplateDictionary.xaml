<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:local="clr-namespace:Microsoft.Research.SpeechWriter.UI.Wpf"
                    xmlns:lib="clr-namespace:Microsoft.Research.SpeechWriter.Core.Items;assembly=Microsoft.Research.SpeechWriter.Core"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <local:TemplateOpacityCoverter x:Key="TemplateOpacityConverter"/>

    <Style TargetType="Button">
        <Setter Property="MinHeight" Value="50"/>
        <Setter Property="MinWidth" Value="50"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>
    <Style TargetType="TextBlock">
        <Setter Property="Margin" Value="5"/>
        <Setter Property="FontSize" Value="25"/>
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>

    <DataTemplate DataType="{x:Type lib:CommandItem}">
        <TextBlock Text="{Binding FormattedContent}" FontSize="25" FontWeight="Bold" Margin="5"/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type lib:SuggestedWordItem}">
        <local:TileControl Background="LightYellow" Caption="{Binding FormattedContent}" IsPrefix="{Binding Tile.IsPrefix}" IsSuffix="{Binding Tile.IsSuffix}"/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type lib:HeadWordItem}">
        <local:TileControl Background="LightGreen" Caption="{Binding FormattedContent}" IsPrefix="{Binding Tile.IsPrefix}" IsSuffix="{Binding Tile.IsSuffix}"/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type lib:ReplacementItem}">
        <StackPanel Orientation="Horizontal">
            <local:TileControl Background="LightGreen" Caption="{Binding OldUnformattedContent}" IsPrefix="{Binding OldIsAttachedToNext}" IsSuffix="{Binding OldIsAttachedToPrevious}"/>
            <TextBlock Text="&#x2192;" FontSize="25" FontWeight="Bold" Foreground="DarkGray" VerticalAlignment="Center"/>
            <local:TileControl Background="LightGreen" Caption="{Binding UnformattedContent}" IsPrefix="{Binding IsAttachedToNext}" IsSuffix="{Binding IsAttachedToPrevious}"/>
        </StackPanel>
    </DataTemplate>
    <DataTemplate DataType="{x:Type lib:GhostWordItem}">
        <local:TileControl Background="LightBlue" Caption="{Binding FormattedContent}" IsPrefix="{Binding Tile.IsPrefix}" IsSuffix="{Binding Tile.IsSuffix}"/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type lib:InterstitialNonItem}">
        <TextBlock Text="-" FontSize="25" FontWeight="Bold" Margin="5"/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type lib:InterstitialGapItem}">
        <TextBlock Text=":" FontSize="25" FontWeight="Bold" Margin="5"/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type lib:InterstitialSpellingItem}">
        <TextBlock Text="*" FontSize="25" FontWeight="Bold" Margin="5"/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type lib:HeadStartItem}">
        <TextBlock Text="&#xAB;" FontSize="25" FontWeight="Bold" Margin="5"/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type lib:TailStopItem}">
        <TextBlock Text="&#xBB;" FontSize="25" FontWeight="Bold" Margin="5"/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type lib:GhostStopItem}">
        <TextBlock Text="&#xBB;" FontSize="25" FontWeight="Bold" Margin="5"/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type lib:SuggestedSpellingItem}">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding Prefix}" FontSize="25" FontWeight="Bold" Foreground="DarkGray" VerticalAlignment="Center"/>
            <Border BorderBrush="Black" BorderThickness="1" Background="LightSteelBlue" Margin="5,2">
                <TextBlock Text="{Binding Symbol}" FontSize="25" FontWeight="Bold" Margin="7,0" Foreground="Black"/>
            </Border>
        </StackPanel>
    </DataTemplate>
    <DataTemplate DataType="{x:Type lib:SuggestedSpellingSequenceItem}">
        <Border BorderBrush="Black" BorderThickness="1" Background="LightSteelBlue" Margin="5,2">
            <TextBlock Text="{Binding Symbol}" FontSize="25" FontWeight="Bold" Margin="7,0" Foreground="Black"/>
        </Border>
    </DataTemplate>
    <DataTemplate DataType="{x:Type lib:SuggestedSpellingWordItem}">
        <Border BorderBrush="Black" BorderThickness="1" Background="LightYellow" Margin="5,2">
            <TextBlock Text="{Binding FormattedContent}" FontSize="25" FontWeight="Bold" Margin="7,0" Foreground="Black"/>
        </Border>
    </DataTemplate>
    <DataTemplate DataType="{x:Type lib:SuggestedSpellingBackspaceItem}">
        <TextBlock Text="{Binding Prefix}" FontSize="25" FontWeight="Bold" Foreground="DarkGray" VerticalAlignment="Center"/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type lib:InterstitialUnicodeItem}">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="&#xE775;" FontSize="25" FontFamily="Segoe MDL2 Assets" FontWeight="Bold" Margin="7,0" Foreground="Black"/>
        </StackPanel>
    </DataTemplate>
    <DataTemplate DataType="{x:Type lib:SuggestedUnicodeItem}">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding Prefix}" FontSize="25" FontWeight="Bold" Foreground="DarkGray" VerticalAlignment="Center"/>
            <Border BorderBrush="Black" BorderThickness="1" Background="LightSteelBlue" Margin="5,2">
                <TextBlock Text="{Binding Symbol}" FontSize="25" FontWeight="Bold" Margin="7,0" Foreground="Black"/>
            </Border>
            <TextBlock Text="{Binding Code, StringFormat={}&amp;#{0};}" FontSize="25" FontWeight="Bold" Margin="7,0" Foreground="Black"/>
        </StackPanel>
    </DataTemplate>

</ResourceDictionary>